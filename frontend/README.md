# Multiomics GraphRAG Agent Platform Frontend

Vue3 + JavaScript + CSS Vite frontend for the intelligent agent management workbench.

## Run

```powershell
npm install
npm run dev
```

The Vite dev server proxies `/api` to `http://localhost:18020` by default for this local workspace.
